--------------------------------------------------------------
ToDo:
	Goto Line 299 SyntaxChecker.java
	update_line_number() is not working for validate_ifs()
	Validate if and while loops ...
--------------------------------------------------------------

--------------------------------------------------------------
NotDone:
--------------------------------------------------------------

--------------------------------------------------------------
Alg := 1) Split the input into tokens. But only partially which does not
			 include split with :=, = ... etc

		 2) Assign appropriate enums to every token.
		 3) Find all struct names and vars inside it and store it in
		 4) Find all the function signatures and store then in a func_info
		 struct_info.

Note: Have a List<Scope> for every function. Every ScopeStack should have
		Global has default.
		???? How should scopes be named ??????

--------------------------------------------------------------
