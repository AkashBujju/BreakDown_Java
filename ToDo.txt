--------------------------------------------------------------
ToDo:
	0) Do in-built functions.
		a) make_objects(type: Type, size: num) -> Type*;
		b) free_objects(ptr: Type*) -> void;
		c) print(s: string, ... primitive types);
		d) scan(s: string, .. primitive types);

	1) Take away quotes_range_indices if possible ... There seems to be
	problems with it !!!!!!!

	2) From where does the relative path of files start, inside the code.....
	??????
--------------------------------------------------------------

--------------------------------------------------------------

Done:
	1) Split into tokens
	2) SyntaxAnalysis and form Info List
--------------------------------------------------------------
